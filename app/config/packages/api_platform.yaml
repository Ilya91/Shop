api_platform:
    collection:
        pagination:
            items_per_page: 10
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
        #multipart: ['multipart/form-data']
#    swagger:
#        api_keys:
#            apiKey:
#                name: Authorization
#                type: header
